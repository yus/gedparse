# gedparse *is…*

GEDCOM 5.5.1 → 7.0 migration tool with full validation

## Features
- Parse GEDCOM 5.5.1 and 7.0 files
- Convert 5.5.1 to 7.0 with all migration rules
- Validate files and generate warnings
- Interactive console UI

## Usage
```bash
# Run directly (after npm ci in CI)
npm start

# Run tests
npm test
